"use strict";
const mongoose = require('mongoose');
const Schema = mongoose.Schema;


let ${1:ModelName} = new Schema({
  name:{
    type:String,
    required:true
  }
});

// export the model
module.exports = mongoose.model('${1:ModelName}', ${1:ModelName});